/**
 * Feature flags — toggle without removing implementations.
 * Set to `true` to show the blog grid on the home page and full blog listing/detail routes.
 */
export const SHOW_BLOG = false;

/** Set to `true` to show the Pricing link in the main nav and footer. */
export const SHOW_PRICING = false;
